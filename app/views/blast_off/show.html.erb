<div class="numbers-game">
  <h1>Blast Off!</h1>
  
  <div class="target-section">
    <h2>Target Number</h2>
    <div class="target-display"><%= @target %></div>
  </div>

  <div class="numbers-section">
    <h3>Use these numbers:</h3>
    <div class="numbers-grid">
      <% @numbers.each do |num| %>
        <div class="number-tile"><%= num %></div>
      <% end %>
    </div>
  </div>

  <div class="operators-section">
    <h3>Available operators:</h3>
    <div class="operators">
      <span class="operator">+</span>
      <span class="operator">−</span>
      <span class="operator">×</span>
      <span class="operator">÷</span>
      <span class="operator">( )</span>
    </div>
  </div>

  <% if flash[:success] %>
    <div class="alert alert-success"><%= flash[:success] %></div>
  <% end %>
  
  <% if flash[:error] %>
    <div class="alert alert-error"><%= flash[:error] %></div>
  <% end %>

  <%= form_with url: blast_off_check_path, method: :post, class: "answer-form" do |f| %>
    <div class="input-group">
      <label>Your solution:</label>
      <%= f.text_field :expression, 
          placeholder: "e.g., (100 + 25) * 2 - 50", 
          class: "expression-input",
          autocomplete: "off" %>
    </div>
    <%= f.submit "Check Answer", class: "btn-submit" %>
  <% end %>

  <div class="instructions">
    <h4>Rules:</h4>
    <ul>
      <li>Use each number only once</li>
      <li>You can use +, -, *, / and parentheses</li>
      <li>You don't have to use all numbers</li>
    </ul>
  </div>

  <%= link_to "New Game", blast_off_path, class: "btn-new-game" %>
</div>

<!-- STYLESHEET -->
<style>
.numbers-game {
  max-width: 600px;
  margin: 40px auto;
  padding: 20px;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
}

.numbers-game h1 {
  text-align: center;
  color: #2c3e50;
  margin-bottom: 30px;
}

.target-section {
  text-align: center;
  margin-bottom: 30px;
}

.target-display {
  font-size: 72px;
  font-weight: bold;
  color: #e74c3c;
  background: #fff3cd;
  border: 4px solid #ffc107;
  border-radius: 10px;
  padding: 20px;
  display: inline-block;
  min-width: 200px;
}

.numbers-section {
  margin-bottom: 30px;
}

.numbers-section h3 {
  color: #34495e;
  margin-bottom: 15px;
}

.numbers-grid {
  display: flex;
  gap: 10px;
  justify-content: center;
  flex-wrap: wrap;
}

.number-tile {
  background: #3498db;
  color: white;
  font-size: 32px;
  font-weight: bold;
  width: 80px;
  height: 80px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 8px;
  box-shadow: 0 4px 6px rgba(0,0,0,0.1);
}

.operators-section {
  text-align: center;
  margin-bottom: 30px;
}

.operators {
  display: flex;
  gap: 15px;
  justify-content: center;
  margin-top: 10px;
}

.operator {
  background: #95a5a6;
  color: white;
  font-size: 24px;
  padding: 10px 20px;
  border-radius: 5px;
}

.answer-form {
  background: #ecf0f1;
  padding: 20px;
  border-radius: 8px;
  margin-bottom: 20px;
}

.input-group {
  margin-bottom: 15px;
}

.input-group label {
  display: block;
  margin-bottom: 8px;
  font-weight: 600;
  color: #2c3e50;
}

.expression-input {
  width: 100%;
  padding: 12px;
  font-size: 18px;
  border: 2px solid #bdc3c7;
  border-radius: 5px;
  box-sizing: border-box;
}

.btn-submit, .btn-new-game {
  background: #27ae60;
  color: white;
  padding: 12px 30px;
  border: none;
  border-radius: 5px;
  font-size: 16px;
  font-weight: 600;
  cursor: pointer;
  display: inline-block;
  text-decoration: none;
}

.btn-submit:hover, .btn-new-game:hover {
  background: #229954;
}

.btn-new-game {
  background: #3498db;
  display: block;
  text-align: center;
  margin-top: 20px;
}

.btn-new-game:hover {
  background: #2980b9;
}

.alert {
  padding: 15px;
  border-radius: 5px;
  margin-bottom: 20px;
  font-weight: 600;
}

.alert-success {
  background: #d4edda;
  color: #155724;
  border: 1px solid #c3e6cb;
}

.alert-error {
  background: #f8d7da;
  color: #721c24;
  border: 1px solid #f5c6cb;
}

.instructions {
  background: #e8f4f8;
  padding: 15px;
  border-radius: 5px;
  margin-top: 20px;
}

.instructions h4 {
  margin-top: 0;
  color: #2c3e50;
}

.instructions ul {
  margin-bottom: 0;
}
</style>
